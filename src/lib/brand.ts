// Re-export of the brand config for spec compatibility.
// Canonical source lives in src/config/brand.ts.
export { brand, TIER_LABEL } from "@/config/brand";
export type { Brand } from "@/config/brand";
